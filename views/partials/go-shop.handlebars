{{!-- The shopping list --}}

<div class="container mt-5" id="goshoplist">
    <div class="row d-flex justify-content-center">
        <h1 class="listtitle my-3">Go Shopping!</h1>
    </div>
    <hr>
    <div class="row">
        <div class="col-lg-6 col-md-12">
            <div class="cow mb-4">
                <h3><i class="fas fa-cheese"></i>   Dairy</h3>
                <ul id="theDairy">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 1 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}
                
                </ul>
            </div>
            <div class="veggies mb-4">
                <h3><i class="fas fa-carrot"></i>   Produce</h3>
                <ul id="TheProduce">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 2 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}

                </ul>
            </div>
            <div class="meat mb-4">
                <h3><i class="fas fa-bacon"></i>   Meat</h3>
                <ul id="TheMeat">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 3 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}

                </ul>
            </div>
            <div class="bread mb-4">
                <h3><i class="fas fa-bread-slice"></i>   Bread/Bakery</h3>
                <ul id="TheBread">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 4 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}

                </ul>
            </div>
        </div>
        <div class="col-lg-6 col-md-12">
            <div class="frozen mb-4">
                <h3><i class="far fa-snowflake"></i>   Frozen</i></h3>
                <ul id="TheFrozen">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 5 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}

                </ul>
            </div>
          <div class="packaged mb-4">
                <h3><i class="fas fa-box-open"></i>   Packaged/Dry Goods</h3>
                <ul id="ThePackaged">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 6 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}

                </ul>
            </div>  
            <div class="other mb-4">
                <h3><i class="fas fa-utensils"></i>   Other</h3>
                <ul id="TheOther">

                    {{#each listItem}}
                        {{#ifEquals this.Item.CategoryId 7 }}
                        {{#unless obtained}}
                        {{#unless nextTime}}
                            <li>
	                            {{Item.name}}
                                <button class="delete-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-cart-arrow-down"></i></button>
                                <button class="next-time border-0 rounded-circle ml-2" data-id="{{this.listItem}}"><i class="fas fa-share-square"></i></button>
                            </li>
                        {{/unless}}
                        {{/unless}}
                        {{/ifEquals}}
                    {{/each}}
                    
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="container mt-5" id="goshoplist">
    <div class="row d-flex justify-content-center">
        <h2 class="nexttime my-3">Get Next Time!</h2>
    </div>
    <hr>
    <div class="nexttime-items pb-4">
        {{#each listItem}}
            {{#if nextTime}}
                <li>
                    {{Item.name}}
                    <button class="undo-nexttime border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-undo-alt"></i></button>
                </li>
            {{/if}}
        {{/each}}        
    </div>
</div>

<div class="container my-5" id="goshoplist">
    <div class="row d-flex justify-content-center">
        <h2 class="obtained my-3">Got it!</h2>
    </div>
    <hr>
    <div class="obtained-items pb-4">
        {{#each listItem}}
            {{#if obtained}}
                <li>
                    {{Item.name}}
                    <button class="readd-listitem border-0 rounded-circle ml-3" data-id="{{this.listItem}}"><i class="fas fa-undo-alt"></i></button>
                </li>
            {{/if}}
        {{/each}}        
    </div>
</div>

<div class="container d-flex justify-content-center">
    <button class="done-shop btn-lg btn-outline-success mb-5 border-0"><i class="fas fa-check"></i>   Done Shopping!</button>
</div>


